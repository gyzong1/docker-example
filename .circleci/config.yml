version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     # start proprietary DB using private Docker image
     # with credentials stored in the UI
     
     # build the application 
     - run: docker login -u admin -p password 13.229.71.253:8081
     - run: docker version
     - run: docker build -t company/app:test .
     
